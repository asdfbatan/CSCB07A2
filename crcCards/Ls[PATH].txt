Class names: Ls[PATH]
Parent class (if any): Commands
Subclasses (if any): 

Responsibilities:
* Print the names of files and directories in the in the current directory, with a new line following each of them.
* If p specifies a file, print p
* If p specifies a directory, print p, a colon, then the contents of that directory, then an extra new line
* If p does not exist, print error message

Collaborators:
* Directory
* Cd